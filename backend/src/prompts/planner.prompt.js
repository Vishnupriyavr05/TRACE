/**
 * @fileoverview Planner agent prompt template (string storage only).
 *
 * Future responsibility: hold the system/user prompt text used by the Planner
 * to interpret research intent and produce an execution plan.
 * Keep templates here; LLM I/O stays in ai/llm and ai/reasoning.
 *
 * Do not implement real prompt content yet.
 */

/** @type {string} */
export const PLANNER_SYSTEM_PROMPT = ''

/** @type {string} */
export const PLANNER_USER_PROMPT_TEMPLATE = ''
